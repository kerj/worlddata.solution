<!DOCTYPE html>
<html>
<head>
  <meta charset='utf-8'>
  <title>To Do List</title>
  <link rel='stylesheet' href='https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css'>
  <link rel='stylesheet' href='~/css/styles.css' type="text/css">
</head>
<body>
  <div class="container">
    <div class="jumbotron">
      <h1>To Do List</h1>
      <em>Pick a country to see to do citys</em>
    </div>
    <ul>
    @if (Model.Count == 0)
    {
      <p>No countries have been added yet!</p>
    }
    @if(Model.Count != 0)
    {
      @foreach (var country in Model)
      {
        <li>@country.CountryName</li>
        <li>@country.CountryPopulation</li>
        <li>@country.CountryExpectancy</li>
        <li><a href="/countries/@country.CountryCode" class="btn btn-dark">More Info</a></li>

      }
    }
    </ul>
    <a href="/" class="btn btn-dark">Back to homepage</a>
  </div>
</body>
</html>
